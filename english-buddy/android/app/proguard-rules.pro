# Native callbacks are invoked through explicit Kotlin references. The web
# bridge itself uses WebMessageListener, not reflection or addJavascriptInterface.
-keepattributes SourceFile,LineNumberTable

