# Copy this file to keystore.properties (which is gitignored) and fill it in.
# The existing ExecLingo upload key is currently located at:
storeFile=/Users/umbertogallo/Downloads/execlingoupload.keystore
storePassword=REPLACE_LOCALLY
keyAlias=REPLACE_LOCALLY
keyPassword=REPLACE_LOCALLY

